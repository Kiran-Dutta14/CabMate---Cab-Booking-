export interface Driver {
  id?: number;            // Auto-generated by backend
  name: string;
  phone: string;
  email: string;
  licenseNumber: string;
  password: string;

  // ✅ Newly added fields to match backend + frontend table
  status?: string;        // "Active" | "Suspended"
  rating?: number;        // Driver rating
  earnings?: number;      // Total earnings
  joined?: string;        // Date joined (ISO string from backend)

  // ✅ Relationships
  rides?: any[];          // RideBooking[] (can be typed later)
  vehicle?: any;          // Vehicle object (can be typed later)
}
